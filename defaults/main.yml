# The Python version, the role supports 3.11 and 3.12
python_version: "3.12"
# The base location of the Virtual environment
virtualenv_basedir: "/opt"
# service name, e.g. omero-server
service_name: ""
# The virtual environment name
env_name: venv3